/**
 * Scenario: {{SCENARIO_NAME}}
 * Generated by API Sequence Runner
 */

const { validators, extractors } = require("api-sequence-runner");

module.exports = {
  name: "{{SCENARIO_NAME}}",
  description: "{{SCENARIO_DESCRIPTION}}",

  steps: [
    {
      name: "step1",
      method: "GET",
      url: "/api/example",

      // validation - choose one or combine multiple
      validate: validators.status(200),
      // validate: (response) => response.status === 200,
      // validate: { statusCode: validators.status(200), hasData: validators.hasData },

      // extract variables for use in subsequent steps
      extract: {
        // simple path extraction
        // exampleId: "data.id",
        // complex extraction with transformation
        // processedData: {
        //   path: "data.value",
        //   transform: (value) => value.toUpperCase(),
        //   default: "DEFAULT_VALUE"
        // }
      },
    },

    // add more steps here
    // {
    //   name: "step2",
    //   method: "POST",
    //   url: "/api/items",
    //   body: {
    //     name: "Test Item",
    //     parentId: "{exampleId}"  // Use extracted variable
    //   },
    //   validate: validators.status(201),
    //   extract: {
    //     itemId: "data.id"
    //   }
    // }
  ],
};
